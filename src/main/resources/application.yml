server:
  port: 8888
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri:
          default-label: main
          search-paths:
            - 'reserva*'
            - 'cadastros-basicos*'


