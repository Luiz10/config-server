server:
  port: 8888
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Luiz10/config-server
          default-label: main
          search-paths:
            - 'reserva*'
            - 'cadastros-basicos*'


